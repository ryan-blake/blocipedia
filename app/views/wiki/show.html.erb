<!-- <div>
  <h1>
    <%= @wiki.first.title %> <br>
    <small>
      submitted <%= time_ago_in_words(@wiki.created_at) %> ago by <%= @wiki.user.email %>
    </small>
  </h1>
</div>

<div class="row">
  <div class="col-md-8">
    <p><%= @wiki.first.body %></p>
  </div>
</div> -->

<!-- <h1><%= @wiki.name %></h1> -->

<%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>

<div class="row">
  <div class="col-md-8">
    <p class="lead"><%= @topic.description %></p>
<!-- #10 -->
    <% @topic.posts.each do |post| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to wiki, wiki %>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">
    <%= link_to "New Post", new_wiki_path, class: 'btn btn-success' %>
  </div>
</div>
